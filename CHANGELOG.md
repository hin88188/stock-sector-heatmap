# 更新日誌 (Changelog)

本文件記錄所有版本的更新內容。格式基於 [Keep a Changelog](https://keepachangelog.com/zh-TW/1.0.0/)。

## [v1.5.4] - 2026-02-05

### 新增
- 📱 **手機版橫向模式優化**：
  - 實作「左右分欄」佈局 (Split View)：左側顯示詳細資訊，右側顯示走勢圖，解決垂直空間不足問題。
  - 圖表高度自動最大化，確保 K 線圖完整顯示且不會被裁切。
- ✨ **垂直時間切換按鈕**：
  - 手機橫向模式下，時間範圍按鈕 (1D/5D...) 自動移至圖表右側垂直排列。
  - 按鈕寬度優化 (104px)，確保長文字 (如漲跌幅) 完整顯示並自動換行。

### 優化
- 🎨 **圖表響應式體驗**：
  - 修復 `StockChart` 在視窗縮放時的重繪邏輯，確保畫布大小即時適應容器變化。
- 🎨 **捲動體驗改善**：
  - 為時間按鈕列表加入 `overscroll-y-contain`，消除捲動到底部時的頁面回彈效果 (Rubber-band effect)。

---

## [v1.5.3] - 2026-02-04

### 新增
- ✨ **Tab 導航與資訊區塊**：
  - 在股票詳情頁新增「圖表」與「資訊」Tab，支援點擊平滑滾動定位。
  - 滾動內容時 Tab 狀態會自動同步更新。
  - **資訊區塊**：按規範順序顯示 30+ 項股票詳細數據（包括市盈率、市淨率、換手率、振幅等）。
- ✨ **API 整合**：
  - 新增 `stock/detail` API 呼叫，獲取更完整的股票即時數據。
  - 實作數據解析與衍生欄位計算（如 PE TTM/動/靜、總市值、漲跌額等）。

### 優化
- 🎨 **詳情頁 Header 重構**：
  - 價格區域重新設計：顯示漲跌箭頭 (↑/↓)。
  - 數據格式優化：漲跌額顯示正負號 (`+`/`-`)，漲跌幅加括號。
  - 視覺統一：整體色系根據漲跌統一為綠色或紅色。
- 🎨 **資訊區塊顯示優化**：
  - **智能配色**：今開、最高、最低價格與昨收比較，高於昨收顯示綠色，低於顯示紅色。
  - **市場適配**：總市值單位自動根據市場顯示 `USD` 或 `HKD`。
  - **代碼格式**：港股代碼保留完整 5 位數格式 (如 `00700.HK`)。
  - **格式微調**：振幅與換手率百分比不顯示正負號。
- 📱 **行業成分股列表優化**：
  - 改為 **覆蓋層 (Overlay)** 模式展開，解決手機版空間不足問題。
  - 修復列表標題列 (Sticky Header) 在滾動時無法鎖定的問題。

---

## [v1.5.2] - 2026-02-04

### 新增
- ✨ 股票詳情頁 (StockDetailModal) 新增行業板塊 (SectorCard) 顯示：
  - 位於 Header 區域，顯示行業名稱、成分股數量與漲跌幅
  - 點擊行業板塊可展開/收合該行業的所有成分股明細 (StockTable)
  - 方便使用者快速查看同板塊其他股票的表現

### 優化
- 🎨 優化股票詳情頁佈局：
  - 將時間範圍選擇器 (TimeRangeSwitcher) 移至走勢圖下方，符合視覺動線
  - 將行業板塊整合至 Header 區域 (分隔線上方)，提升資訊層級
  - 調整 Header 內部結構，確保 Logo、名稱、價格與行業板塊排列整齊

---

## [v1.5.1] - 2026-02-03

### 修復
- 🐛 修復股票走勢圖 X 軸可被左右拖曳移動的問題：
  - 禁用 `handleScroll` 與 `handleScale` 防止用戶拖曳或縮放圖表
- 🐛 修復窄螢幕下 5D 等長時間範圍數據無法完整顯示的問題：
  - 設定 `minBarSpacing: 0.001` 允許極高數據密度
  - 設定 `fixLeftEdge: true` 與 `fixRightEdge: true` 固定顯示範圍邊界
  - 使用 `setVisibleRange()` 取代 `fitContent()` 強制顯示完整時間範圍
  - 新增 `dataRef` 供 ResizeObserver 在視窗調整時重新設定可視範圍

---

## [v1.5.0] - 2026-02-03

### 新增
- ✨ 新增股票詳情視窗 (StockDetailModal)：
  - 點擊成交排行中的任意股票列即可開啟詳情視窗
  - 顯示股票名稱、代號、當前價格與漲跌幅
  - 支援 ESC 鍵或點擊背景關閉視窗
- ✨ 新增互動式走勢圖 (StockChart)：
  - 使用 TradingView lightweight-charts 4.2 Baseline Series
  - 支援 1D / 5D / 1M / 3M / 6M / 1Y 六個時間範圍切換
  - 綠色區域表示高於基準價，紅色區域表示低於基準價
  - 支援滑鼠滾輪縮放與拖曳平移
- ✨ 新增浮動提示框 (Floating Tooltip)：
  - 滑鼠移動時 tooltip 浮動於數據點上方
  - 顯示股票名稱、價格、漲跌幅（基於 baseline）、時間
  - 自動適應深色/淺色模式
- ✨ 新增時間範圍切換器 (TimeRangeSwitcher)：
  - 響應式 Grid 佈局：小螢幕 3×2、大螢幕 6×1
  - 每個按鈕顯示時間範圍與該期間漲跌幅
  - 當前選中範圍以藍色背景標示

### 優化
- 🌐 圖表時區與瀏覽器一致：
  - X 軸刻度使用 `tickMarkFormatter` 搭配 `toLocaleTimeString()`
  - Crosshair 標籤使用 `localization.timeFormatter`
  - Tooltip 時間使用瀏覽器本地時區格式化
- 📊 改進 baseline 計算邏輯（參考官方 MiniTimesharesSparklineWidget）：
  - 1D：baseline = 昨收價 (pre_close)
  - 5D：baseline = 第一個交易日的昨收價
  - 1M/3M/6M/1Y：baseline = 第一筆 K 線收盤價

### 技術實作
- 🔧 新增 `parseChartData()` 函數，返回 `{ points, baseline }` 結構
- 🔧 新增 `StockChart` 元件，支援 `data`、`baseline`、`stockName` props
- 🔧 新增 `TimeRangeSwitcher` 元件，支援響應式 Grid 佈局
- 🔧 新增 `StockDetailModal` 元件，整合走勢圖與時間切換
- 🔧 新增 `CHART_APIS` 常數定義各時間範圍的 API 端點
- 🔧 使用 `line_type=1000` 作為日 K 線參數（修正原先錯誤的 `line_type=1`）

---

## [v1.4.4] - 2026-02-02

### 新增
- ✨ 新增語言切換功能，支援繁體中文、簡體中文、英文：
  - 在設定面板新增語言選擇區塊（位於外觀模式下方）
  - 透過 `locale` URL 參數控制 API 回傳的股票名稱語言
  - 預設語言為繁體中文 (zh-HK)
  - 語言設定自動保存至 localStorage
  - 切換語言後自動重新載入資料

### 優化
- ⚡ 簡化 CORS Proxy 設定，僅使用單一 proxy 服務
- 🎨 語言選擇 UI 採用與外觀模式一致的按鈕群組設計

### 技術實作
- 🔧 新增 `LANGUAGES` 常數定義 (zh-HK, zh-CN, en)
- 🔧 新增 `language` state 與 localStorage 持久化
- 🔧 修改 `fetchData()` 與 `fetchOtherMarketData()` 加入 `locale` 參數
- 🔧 新增語言切換時自動重新抓取資料的 useEffect

---

## [v1.4.3] - 2026-01-27

### 新增
- ✨ 實作動態凍結表頭功能：
  - 向下滾動時 Header 隱藏，表頭凍結於螢幕頂部
  - 向上滾動時 Header 顯示，表頭凍結於 Header 下方
  - 使用 ResizeObserver 動態測量 Header 高度（支援字型縮放 50%-200%）
  - 適用於「成交排行」和「強弱勢排行」兩種視圖

### 優化
- 🎨 全面重構「成交排行（TurnoverList）」顯示樣式：
  - **展開模式 (Expanded Mode)**：
    - 價格與漲跌幅改為單行並排顯示，提升閱讀效率
    - 價格欄位設定固定寬度並靠右對齊，確保垂直視覺整齊劃一
    - 移除彈性寬度限制，釋放更多空間予左側股票名稱與代號
  - **收起模式 (Collapsed Mode)**：
    - 價格字型大小調整為與展開模式一致 (`text-sm`)
    - 價格顏色統一改為預設文字顏色（明亮/暗黑模式適應），不再隨漲跌變色
    - 移除右側數據區最小寬度限制，最大化股票資訊顯示空間
  - **漲跌幅顯示升級**：
    - 改為圓角色塊（Pill）樣式，白字配深色背景
    - 使用全新的專屬配色：綠(`#06A870`) / 紅(`#F03A55`) / 灰(`#646F85`)
    - 標籤內文字水平與垂直完全置中

---

## [v1.4.2] - 2026-01-22

### 修復
- 🐛 修復暗黑模式下成交排行股票列 hover 時，因背景透明度導致表頭欄位（Vol、PE、RS、Industry）文字透出的問題

---

## [v1.4.1] - 2026-01-22

### 新增
- ♿ 無障礙性支援：
  - 為所有 icon-only 按鈕添加 `aria-label`
  - 為搜尋輸入框添加關聯的 `<label>`
  - 添加 `focus-visible` 樣式提供清晰的鍵盤焦點指示
  - 支援 `prefers-reduced-motion` 尊重使用者動態偏好
- ✨ 新增 Skeleton Loading 元件，提升初次載入視覺體驗
- ✨ 新增 `.sr-only` 類別支援螢幕閱讀器

### 優化
- ⚡ React 效能優化：
  - 為 `MarketBreadth`、`SectorCard`、`MiniSectorCard` 添加 `React.memo`
  - 使用 `useCallback` 優化 `handleSectorClick` 等 event handlers
  - 使用 functional updates 優化 setState 呼叫
- 🎨 替換設定面板中的 Emoji（☀️、🌙、🔄）為 SVG 圖示

### 修復
- 🐛 移除 viewport 的 `user-scalable=no` 限制，允許使用者縮放頁面

---

## [v1.4.0] - 2026-01-22

### 新增
- ✨ 新增用戶偏好記憶功能，自動保存以下設定：
  - 市場選擇（美股/港股）
  - 排行模式（成交/強勢/弱勢）
  - 時間週期（1D/5D/10D/1M/6M/YTD）
- ✨ 新增「重置所有設定」按鈕於設定面板
- ✨ 實作動態字型縮放系統（混合方案）：
  - 字型 ≤100%：維持固定行高和欄位寬度
  - 字型 >100%：行高和欄位寬度隨字型自動縮放

### 優化
- 🎨 改善 TurnoverList UI/UX 設計（參考現代設計規範）：
  - 優化 Sticky 欄位陰影效果
  - 改善 Hover 和 Active 互動狀態
  - 統一明亮/暗黑模式的邊框顏色
- 🎨 VOL、PE、RS、Industry 標題和數據改為靠右對齊
- 🎨 INDUSTRY 欄位內容卡片自動填滿欄位寬度
- 📐 標題列與數據列欄位寬度完美同步對齊

### 修復
- 🐛 修復標題列與資料之間的空白問題
- 🐛 修復虛擬滾動初始化時底部空白問題
- 🐛 修復字型放大時文字重疊問題
- 🐛 修復字型放大時 VOL、PE、RS 數據擠壓問題

---

## [v1.3.0] - 2026-01-15

### 新增
- ✨ 新增設定面板（齒輪圖示），整合外觀模式與字型大小設定
- ✨ 新增字型大小調整功能 (50%-200%)，支援 +/- 按鈕操作
- ✨ 新增回到頂部浮動按鈕，滾動時顯示，5秒自動隱藏
- ✨ 成交排行展開/收起模式狀態自動保存
- 💾 所有設定自動保存至 localStorage

### 優化
- 🎨 展開模式名稱欄位響應式寬度，優先縮小以保持固定欄位完整
- 🎨 強勢/弱勢排行板塊欄位改為響應式寬度 (140px ~ 50vw)
- 🎨 回到頂部按鈕位置隨導航欄顯示/隱藏動態調整

### 修復
- 🐛 修正展開模式下搜尋高亮效果未正確顯示的問題

---

## [v1.2.0] - 2026-01-14

### 新增
- ✨ 新增搜尋股票功能，支援跨市場模糊搜尋
- ✨ 點擊搜尋結果自動滾動定位並高亮顯示股票
- ✨ 支援鍵盤導航 (↑↓ 選擇, Enter 確認, Esc 關閉)

### 優化
- 🎨 搜尋框搭配毛玻璃效果，符合現有 UI 風格
- 🎨 成交排行中股價顏色改為跟隨漲跌幅顏色

---

## [v1.1.0] - 2025-12-13

### 新增
- ✨ 新增成交排行視圖作為預設首頁
- ✨ 新增滾動隱藏/顯示導航列功能
- ✨ Split View 高度自適應（成份股少時占用較少空間）

### 優化
- 🎨 添加毛玉璃效果增強視覺體驗

### 修復
- 🐛 修復載入更多股票後 sticky 欄位位移問題

---

## [v1.0.0] - 2025-12-03

### 新增
- 🎉 首次發布
- 實作美股/港股板塊熱力圖
- 實作市值加權漲跌幅計算
- 整合深色/淺色模式切換
